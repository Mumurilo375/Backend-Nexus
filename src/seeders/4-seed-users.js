"use strict";

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up(queryInterface, Sequelize) {
    // Este seeder é duplicado e foi desabilitado. Os dados são inseridos por 3-seed-users.js
  },

  async down(queryInterface, Sequelize) {
    // Este seeder é duplicado e foi desabilitado
  },
};
