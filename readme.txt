npm i sequelize pg pg-hstore dotenv
npm i express
npm i -D nodemon sequelize-cli (permite que o arquivo reatualize sempre que salvarmos ele, sem precisar rodar o server novamente)
criei a src
(na src) npx sequelize-cli init (cria um config.json, uma pasta de models, migrations e seeders)
volta pra pasta raiz, npm i -D typescript ts-node-dev @types/node @types/express
Criei o tsconfig.json e configurei